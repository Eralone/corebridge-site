/**
 * Ключ, под которым страница оплаты запоминает `payment_id` перед уходом
 * на Robokassa.
 *
 * ⚠️ Robokassa возвращает на SuccessURL свой `InvId`, а наружу мы показываем
 * только `public_id` (`InvId` последователен — по нему перебираются чужие
 * платежи). Поэтому идентификатор для поллинга сохраняем сами, а не надеемся
 * получить его из параметров возврата.
 *
 * `sessionStorage`, а не `localStorage`: значение живёт ровно до конца вкладки
 * и не переживает её закрытие. Если хранилище недоступно (приватный режим)
 * или человек вернулся в другой вкладке — страница успеха возьмёт последний
 * платёж из истории.
 */
export const PAYMENT_ID_KEY = 'cb_payment_id';
