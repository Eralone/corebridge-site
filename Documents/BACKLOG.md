# Бэклог работ corebridge-site

> **Это рабочий файл агента, а не отчёт для заказчика.** Читать первым делом в начале
> каждой сессии — здесь состояние, следующая задача и всё, что нельзя потерять при сбросе контекста.
>
> **Правило ведения:** закрыл задачу → сразу вернулся сюда, переставил статус, дописал, что
> реально сделано и что выяснилось по ходу. Не «в конце сессии», а сразу после задачи — иначе
> при обрыве контекста запись потеряется.
>
> Статусы: `[ ]` не начато · `[~]` в работе · `[x]` сделано · `[!]` заблокировано · `[-]` отменено
>
> Последнее обновление: 2026-08-01 (документация: раздел инструкций по .epf,
> путь до JWT-токена со скриншотами; поисковая оптимизация всех страниц)

---

## 0. ✅ Обязательство выполнено: тестовые данные вычищены

**Дмитрий разрешил завести тестовый аккаунт на проде с условием, что в конце
работ он и все его данные вычищаются.** Выполнено 2026-07-30, сверено по базе.

Удалено: тенант `qa-e2@corebridge.ru` (со всеми пользователями, лицензиями,
счётчиками, очередью и обращениями — каскадом), временный админ
`qa-admin@corebridge.ru`, запись `127.0.0.1 admin.corebridge.ru` в `/etc/hosts`.
Проброс `socat` на порт 3003 снят раньше — сервер опубликовал порт сам.
Аккаунты сквозного прогона (`qa-journey-*`) чистятся самим прогоном.

Сверка после чистки: в `platform.tenants` осталось **ровно 4 записи** — те же,
что были до начала работ (два `rls-test-*`, два `epf-test-*`).

⚠️ **Что осталось и почему это правильно.** В `platform.audit_log` сохранился
41 запись тенанта: таблица неизменяема по замыслу, записи должны переживать
удаление аккаунта. Обезличить их сервер не смог — это дефект, описанный
в промте **S15**. Идентификатор в `actor` уже никуда не ведёт: пользователя нет.
Но одна запись (`user_registered`) содержит почту в `new_value`, и её
`purgeTenant` не трогает даже по замыслу — второй пункт того же промта.

---

## 1. Что это за проект (в двух абзацах)

Превратить 27 готовых HTML-страниц дизайна в работающий сайт `corebridge.ru` на Next.js —
визуально **1:1** с макетом, с живым бэкендом. Бэкенд (`/opt/corebridge`, репозиторий
`corebridge-server`) уже реализовал почти всё нужное API; сайт его потребляет.

Дизайн — это **референс раскладки, шрифтов и форм**. Значения внутри (имена, суммы, счётчики,
названия компаний) — иллюстративные заглушки. Вёрстку переносим 1:1, данные подставляем из API.

---

## 2. Жёсткие ограничения — нарушать нельзя

| # | Ограничение | Почему |
|---|---|---|
| 1 | **`/opt/corebridge` не править руками** | автодеплой с локального ПК затрёт. Нужны правки бэкенда → промт в `Documents/prompts/` |
| 2 | **`/lk/*` занят API** | nginx проксирует в lk-api:3000. Экраны ЛК под этим префиксом размещать нельзя → `/dashboard`, `/epf`, а не `/lk/dashboard` |
| 3 | **`/admin/*` занят API** | nginx проксирует в admin:3003. На `admin.corebridge.ru` UI живёт в корне, разводит `middleware.ts` |
| 4 | **Никакого Tailwind и shadcn** | их preflight переопределяет `site.css` и ломает попиксельное совпадение (`implementation_strategy.md` §3) |
| 5 | **`design-source/` только на чтение** | это эталон. Правки живут в реализации, а не в эталоне |
| 6 | **Прайс не хардкодить** | только `GET /lk/plans`. Канон цифр — сервер (S2 привёл каталог к `pricing.html`) |
| 7 | **Не публиковать выдуманные данные** | `contacts.html` (реквизиты несуществующего ООО) и `for-business.html` (отзывы от имени выдуманных людей) — блоки не переносим |
| 8 | **`listen [::]:443` не писать** в своих vhost | nginx слушает 443 только на IPv4; иначе vhost станет default для IPv6 и отдаст свой сертификат чужому домену |
| 9 | **SMS не существует и не появится** | второй фактор и уведомления только через Telegram. Колонку SMS в матрице не рисовать |
| 10 | **Каждый экран проверять на трёх ширинах** | требование Дмитрия 2026-07-29: ПК, планшет, телефон. `npm run inspect` снимает `desktop.png` (1440), `tablet.png` (820), `mobile.png` (390) и ловит горизонтальную прокрутку на двух узких. Виновника ищет `node tools/overflow.mjs <url> <mobile\|tablet>`. ⚠️ В эталоне мобильной вёрстки нет вовсе — её приходится делать, а не переносить |

---

## 3. Где искать информацию

| Что нужно | Файл |
|---|---|
| **Полный реестр эндпоинтов сервера** (источник истины) | `Documents/server_ask/API_ENDPOINTS.md` |
| Контракты запросов/ответов | `Documents/server_ask/site_server_integration_reference.md` §9b, §9c |
| Что реализовано пакетами S1–S7 | `Documents/server_ask/site_S1_S7_RESPONSE.md` |
| Что реализовано пакетами S8–S9 | `Documents/server_ask/site_S8_S9_RESPONSE.md` |
| ⚠️ Устаревший документ, сверять с `API_ENDPOINTS.md` | `Documents/server_ask/answers_from_server.md` |
| **Разбор каждой страницы дизайна**: что из API, что убрать | `Documents/design_findings.md` (70 КБ) |
| Точные правки текстов юр. страниц | `Documents/legal_corrections.md` |
| Карта real / mock / blocked | `Documents/gap_analysis.md` |
| Ручные шаги Дмитрия (DNS, Яндекс ID, Robokassa) | `Documents/manual_setup.md` |
| Открытые продуктовые вопросы | `Documents/questions_open.md` |
| Промты бэкенду | `Documents/prompts/backend_S1..S12*.md` |
| Эталон вёрстки | `design-source/*.html` (27 шт.) + `design-source/assets/` |
| Карта «страница → спека P01–P11» | `Documents/design_findings.md`, раздел про `sitemap.html` |
| План этапов и топология | `README.md` |

**Дизайн-проект Claude Design:** `1bcb6b38-c313-411e-8d6b-bcd4c8fc5ba3`.
`DesignSync get_file` доступен **только основной сессии**, субагентам не передаётся.
Лимит `get_file` — 256 KiB.

---

## 4. Сделано

### Инфраструктура — закрыта полностью

- [x] **DNS переведён на VPS.** `corebridge.ru`, `www`, `admin` → `77.90.61.5`. Раньше указывал на Vercel.
- [x] **Сертификаты Let's Encrypt.** `corebridge.ru`+`www` (до 24.10.2026), `admin.corebridge.ru`
      (до 26.10.2026, ECDSA, webroot, deploy-hook `reload nginx`). `api.corebridge.ru` был раньше.
- [x] **vhost `corebridge.ru`** — `deploy/nginx/corebridge.ru.conf`. `/lk/*`→3000, `/`→3005,
      `/internal/`→403 (дыру нашёл сервер, закрыл и проверил).
- [x] **vhost `admin.corebridge.ru`** — `deploy/nginx/admin.corebridge.ru.conf`. `/admin/*`→3003
      с whitelist на **оба** IP Дмитрия, `/`→3005 без фильтра (иначе при смене IP не видно даже логина).
- [x] **Приём почты.** postfix virtual-домен + пересылка на Gmail, без dovecot и IMAP.
      Алиасы: `info@`, `support@`, `privacy@`, `hello@`, `sales@`, `alerts@`, `postmaster@`, `abuse@`.
      Не open relay (`554 Relay access denied`), неизвестные адреса `550` без backscatter.
      MX добавлен, сквозной тест: письмо на `info@` дошло до Gmail (`250 2.0.0 OK`).
      Конфиг — `deploy/mail/virtual`.
- [x] **cron фазы 2 удаления аккаунтов.** `/etc/cron.d/corebridge-purge`, 03:30 UTC,
      обёртка `deploy/cron/corebridge-purge.sh`, лог `/var/log/corebridge-purge.log`.
      ⚠️ Вариант сервера (`docker exec … node scripts/purge-deleted-tenants.js`) **нерабочий** —
      скрипт есть только на хосте, в образ admin не попал. Вызываю `purgeExpired()` из сервиса напрямую.
- [x] **systemd `corebridge-site.service`** — порт 3005, `enabled`, перезапуск проверен.

### Бэкенд — все девять пакетов реализованы и проверены на проде

- [x] S1 auth (register, сброс пароля, verify-email, **Яндекс ID**; Google удалён → 404)
- [x] S2 каталог тарифов по канону `pricing.html` + промо `first30` + `is_perpetual`
- [x] S3 `POST /lk/contact` (не `/api/v1/public/contact`)
- [x] S4 уведомления + Telegram-бот `@CoreBridgeRu_bot`
- [x] S5 сессии (оба шага)
- [x] S6 `GET /admin/users`, `POST /admin/tenants/:id/set-plan`
- [x] S7 `verify` по cookie + флаги `auth_kind`/`needs_reauth`/`reauth_action`
- [x] S8 2FA через Telegram, обращения по ПДн, экспорт и удаление аккаунта
- [x] S9 `GET /admin/health`, `GET /admin/audit`, доборы `/admin/users`
- [x] Миграции 022, 023, 024 применены (сверено по `platform.schema_migrations`)

### Дизайн

- [x] **27 страниц + 8 assets** скачаны байт-в-байт в `design-source/`.
      Лендинг-standalone (456 КБ) выгрузил Дмитрий вручную — превышал лимит MCP.
      Сверен с `index.html`: совпадение 99,6 %, расхождения только в сериализации SVG.
- [x] Разбор всех страниц → `design_findings.md`, правки юр. текстов → `legal_corrections.md`
- [x] Спека P01 → `Documents/design/P01_landing_visual.md`

### Каркас (Э0, частично)

- [x] Удалены `src/`, Tailwind, shadcn, их конфиги
- [x] `app/` по дереву E0: группы `(public)`, `(auth)`, `(lk)`, `(admin)` — **26 маршрутов**
- [x] `app/layout.tsx` — `site.css` без изменений + Inter 400–800 + JetBrains Mono
- [x] `public/assets/` ← копия `design-source/assets/` (site.css, legal.css, robokassa.css)
- [x] `lib/contracts/lk.ts` — типы по фактическому API, включая всё из S8/S9
- [x] `lib/api/client.ts` — fetch с `credentials:'include'`, `ApiError`, коды ошибок из контракта
- [x] `lib/api/lk.ts` — обёртки эндпоинтов
- [x] Разведены коллизии маршрутов: `/integrations` (публичный) vs `/my-integrations` (ЛК),
      `/n8n` (SEO) vs `/workflows` (ЛК)
- [x] `npm run build` зелёный, сайт открывается на `https://corebridge.ru`
- [x] **Э0.2 компоненты из `shell.js`** (2026-07-28). Перенесены с дословной разметкой:
      `Sidebar`, `Topbar`, `AdminSidebar`, `AdminTopbar`, `PublicHeader`, `PublicFooter`,
      `Popup` (портал, замена императивного `CBPopup`), оболочки `LkShell` / `AdminShell`,
      заглушка `StubBody`. `window.CB_USER` → `lib/user-context.tsx` (`UserProvider` +
      `useUser`), данные из `GET /lk/profile`, инициалы вычисляются из имени.
      Проверено на собранном HTML и вживую:
      · `admin.corebridge.ru/users` — сайдбар, топбар, `app--admin`, `admin-badge` на месте,
        активный пункт `<a class="active" href="/users">` подсвечен;
      · `dashboard` — `class="app"`, сайдбар с `/my-integrations` и `/workflows`,
        активный пункт подсвечен, `sidebar-user` отрисован;
      · пункт «Админ-панель» **скрыт** при `role !== 'admin'` (0 вхождений в HTML);
      · **ни одной ссылки на `about.html`** во всей сборке.
      Исправлены баги эталона: подсветка пункта интеграций (`integrations-app` вместо
      `integrations`), `about.html` → `/for-business`, «Выйти» теперь дёргает
      `POST /lk/auth/logout`, а не просто ведёт на `/login`.
      Отступления: пункт «Поддержка» в админ-сайдбаре **не перенесён** (тикет-системы нет,
      поддержка вне MVP, бейдж «12» был заглушкой); зашитый `d.korolev@corebridge.ru`
      в админ-топбаре вынесен в параметр (источник `GET /admin/auth/me`); бейдж
      «Системы в норме» стал параметром (источник `GET /admin/health`).
### Э2 — Dashboard и epf: сделано (2026-07-28)

- [x] **`dashboard`.** KPI, график, лента событий, таблица интеграций и быстрые действия —
      всё на живых данных. `styles/lk.css` — стили из `<style>` макетов дашборда и epf.
      Компоненты `ActivityChart` (path строится из `GET /lk/dashboard/activity`),
      `events.tsx` (перевод кодов `audit_log` в человеческий текст + `timeAgo`).
      Убрано, чего сервер не отдаёт: дельты («+2 за неделю», «+12 % к апрелю»), период **90д**
      (сервер принимает только `7d`/`30d`), `requests_this_month` в строке интеграции → «—».
      «Успешность синхронизации» считается на клиенте как `ok/(ok+error)`.
      «Операций за месяц» из макета честно переименовано в **«Запусков n8n за месяц»**:
      общего счётчика операций на сервере нет (промт S10), выдавать одно за другое нельзя.
      Добавлены состояния, которых в макете не было: пустая лента, пустой список интеграций,
      ошибка загрузки, баннер «вошли кодом восстановления».
- [x] **`epf`.** Выбор конфигурации, JWT, версии сборок, скачивание.
      ⚠️ **Конфигурации проверены у сервера:** `ut11 | unf | ka | bp`. Карточки «1С:ERP»
      из макета не существует (`?config=erp` → `INVALID_CONFIG`), зато есть **БП 3.0**,
      которой в макете не было. Убрано обещание «инструкции в PDF на email» — сервер её не шлёт.
- [x] **Сборок .epf на сервере нет вовсе.** `GET /lk/epf/versions?config=ut11` → `versions: []`,
      `GET /lk/epf/download?config=ut11` → **404 `EPF_NOT_FOUND`**. Экран это показывает честно:
      кнопка скачивания заблокирована с подписью «файл ещё не опубликован», а не ведёт в ошибку.
      **Нужно действие Дмитрия:** выложить сборки через админку.
- [x] **⚠️ `days_left` от сервера доверять нельзя.** На пробном тарифе `GET /lk/dashboard`
      отдаёт `days_left: 0` при бессрочной лицензии (`valid_until: null`, `is_perpetual: true`
      в журнале) — «0 дней» выглядело бы как истёкший доступ. Документация обещала `null`.
      Обходим на клиенте: признак бессрочности — **`valid_until === null`**, а не `days_left`.
- [x] **Мобильная вёрстка макета не проверялась.** Быстрые действия ниже 1024px становятся
      двумя колонками и на 390px не помещаются (426px против 390px). Дописано правило
      в одну колонку; то же для строки интеграции.
- [x] **Обходчик больше не жмёт кнопки, которые что-то меняют.** Клик-проход дёрнул
      `/lk/token/refresh` на живом сервере — спасло только то, что на пробном тарифе там 402.
      Добавлен список запретов (обновить, удалить, отвязать, оплатить, сбросить…).
- [x] **Убрана подмена времени в снимках.** Фиксированная дата превращала свежие события
      в «через 6 месяцев» — ложные данные хуже, чем разброс подписей «3 минуты назад».

### Э5 — Settings: сделано (2026-07-29)

- [x] **Шесть разделов** на боковой навигации: Профиль, Компания, Команда, Безопасность,
      Уведомления, Данные и приватность. Прокликаны все — рендерятся, JS-ошибок нет.
- [x] **Убрано из макета:** блок **API-ключей `cb_*`** целиком (таких ключей на сервере нет,
      доступ к API даёт JWT со страницы `/epf`); **двухфакторка «через SMS»** и **колонка SMS**
      в матрице уведомлений (SMS не существует и не появится); аватар с загрузкой, Должность,
      Язык интерфейса, Часовой пояс — этих полей на сервере нет.
- [x] **Добавлено, чего в макете не было:** раздел «Компания» (поля на сервере есть),
      раздел «Команда» (API готов целиком: приглашение, смена роли, удаление),
      раздел «Данные и приватность» (экспорт и удаление аккаунта из S8).
- [x] **Второй фактор** — двухшаговое включение (`/lk/2fa/enable` → `/lk/2fa/confirm`),
      коды восстановления показываются один раз с явным предупреждением, `recovery_codes_left`
      выводится. Отключение требует пароля. Подписано: «Вход через Яндекс ID не требует кода».
- [x] **Telegram** — привязка через deep link с опросом статуса раз в 2 с (пуша с сервера нет),
      отвязка. Код `409 TELEGRAM_REQUIRED_FOR_2FA` объясняется словами, а не показывается ошибкой.
- [x] **Сеансы** — список с текущим, завершение одного и «выйти на всех других устройствах».
- [x] Разобраны коды ошибок: `USERS_LIMIT_REACHED` (с лимитом), `LAST_OWNER`,
      `CANNOT_DELETE_SELF`, `USER_EXISTS`, `INVALID_PASSWORD`, `OAUTH_ACCOUNT`.
- [x] ⚠️ **Ловушка контракта:** тип обращения по ПДн — `'deletion'`, а не `'delete'`.
- [x] **Ветки ролей проверены переключением роли в базе** (идея Дмитрия: менять роль и тариф
      тестового аккаунта вместо заведения четырёх). Под `manager`: `GET /lk/token/full` → `403`,
      экран `/epf` показывает «токен доступен только владельцу», форма приглашения в «Команде»
      скрыта. Роль возвращена на `owner`. Команды и порядок — `Documents/test_account.md` §4.
- [x] ⚠️ **Роль кэшируется в сессии Redis.** После смены роли `GET /lk/profile` отдаёт новую
      (читает БД), а `GET /lk/auth/session` и проверки прав — старую, пока жива сессия (до суток).
      Понижение роли не отзывает полномочия сразу. На стороне сайта не чинится.
      При прогонах ролей **обязательно перелогиниваться**.
- [x] **Прогон `user` и «без подписки» выполнен** (2026-07-29). Нашлось два расхождения,
      оба исправлены:
      · в «Моих интеграциях» и воркфлоу **кнопки действий показывались роли «только чтение»** —
        сервер отвечал 403, но кнопка обещала то, чего не будет. Теперь скрыты/заблокированы;
      · при погашенной лицензии `/lk/token/full` отдаёт `404 TOKEN_NOT_FOUND`, а экран `/epf`
        писал «Не удалось получить токен, обновите страницу» — как будто сбой. Теперь:
        «Активной лицензии нет… Оформите тариф».
      Таблицы проверенных путей — `test_account.md` §4. Состояние аккаунта возвращено:
      `owner` · `trial` · лицензия активна.
- [x] **Прогон платного тарифа `professional` (2026-07-29).** Три находки, все исправлены:
      · **лимит n8n показывался нулевым на оплаченном тарифе.** `dashboard.n8n_usage.limit`
        читается из `usage_counters`, а строка там создаётся лениво, при первом запуске.
        До него сервер отдаёт `limit: 0` на любом тарифе — сайт писал оплатившему человеку
        «на пробном тарифе n8n недоступен». Лимит теперь берётся из `GET /lk/plans`
        (дашборд, воркфлоу; биллинг и так брал оттуда);
      · **биллинг предлагал купить тариф, который уже оплачен.** На «Профессионале» и кнопка
        в карточке, и баннер справа звали «Попробовать» тот же «Профессионал». Промо
        `once_per_tenant` — оплата упёрлась бы в `PROMO_ALREADY_USED`. Теперь на своём
        промо-тарифе показывается «Продлить на месяц», баннер скрыт; на дашборде подпись
        плитки «Сменить тариф» тоже не рекламирует уже действующий тариф;
      · **`POST /lk/workflows/activate` требует `integration_id`**, а сайт слал только
        `template_id` → гарантированный `400 MISSING_FIELDS` на каждое нажатие «Включить».
        Не всплывало раньше только потому, что каталог всегда пуст (см. S12). Исправлено:
        интеграция выбирается в карточке, кнопка блокируется с объяснением, если подходящей нет.
      Заодно контракты приведены к фактическим ответам сервера: `WorkflowTemplate` —
      `required_integrations`/`tags` вместо выдуманных `category`/`requires`,
      `WorkflowExecution.startedAt` (единственный camelCase в API), `n8n_usage.period`
      и `price.discount_percent` могут быть `null`. Таблица путей — `test_account.md` §4.

### Промты бэкенду S10, S12, S13 — написаны, ждут передачи (2026-07-29)

- [x] **`Documents/prompts/backend_S10_usage.md`** — учёт операций. Плюс две вещи, найденные
      прогоном: `limit_value` в счётчике пишется как `1000` независимо от тарифа, и «лимит 0»
      при отсутствии строки. Один вопрос Дмитрию/серверу внутри: **что считать «операцией»** —
      от ответа зависит место инкремента.
- [x] **`Documents/prompts/backend_S12_workflows.md`** — каталог воркфлоу не работает вовсе:
      · шаблоны лежат на хосте в `/opt/corebridge/n8n/templates`, а сервис ищет их
        в `/n8n/templates` внутри контейнера — тома нет, `getCatalog()` молча отдаёт `[]`.
        **Каталог пуст всегда и для всех**, независимо от тарифа;
      · в самих JSON нет ни `template_id`, ни описания, ни `required_integrations`;
        `name` — плейсхолдер `{TENANT_ID}__{PROJECT_ID}__…`, `tags` — объекты вместо строк.
        Смонтировать том без правки метаданных будет хуже, чем оставить пусто;
      · `status: exec.status || exec.finished ? 'success' : 'running'` — приоритет операторов,
        **упавший сценарий показывается успешным**;
      · `integration_id` обязателен, но не описан в `API_ENDPOINTS.md`.
- [x] **`Documents/prompts/backend_S13_admin.md`** — админка:
      · 🔴 **контейнер `admin` не опубликован на порт**, nginx проксирует в `127.0.0.1:3003`,
        которого нет → **весь `/admin/*` отдавал 502**. С не-whitelisted адреса это
        маскировалось 403 от IP-фильтра, поэтому не всплывало. У остальных четырёх
        сервисов `ports: 127.0.0.1:<port>` есть, у admin — нет;
      · четыре эндпоинта отвечают 500, SQL разошёлся со схемой: `GET /admin/tenants`
        и `/tenants/:id/tokens` (`licenses.issued_at`, `licenses.details`),
        `GET /admin/epf/versions` (`epf_versions.is_public`, `tenant_id`),
        `GET /admin/queues/stats` (`dead_letter_queue.status`, плюс две таблицы
        `dead_letter_queue` с разными колонками — надо свести к одной);
      · вход: при выключенном TOTP сессия всё равно выдаётся только на шаге 2,
        а маршрут требует непустой `totp_code`. Сайт шлёт заглушку.
- [ ] ⏳ Передать Дмитрию S10, S11, S12, S13 для локального claude-code.
- [ ] ⏳ **Вопрос Дмитрию:** у единственного админ-аккаунта `admin@corebridge.ru`
      выключен TOTP. Панель меняет тарифы, блокирует тенантов и запускает удаление
      аккаунтов — вход защищён только паролем и IP-whitelist. Код TOTP готов, включить
      стоит до передачи панели в работу.

### Э7 — Админка: сделано (2026-07-29)

- [x] **Вход.** ⚠️ Cookie `admin_session_id` сервер ставит с `path=/admin`, поэтому
      на запрос самой страницы браузер её не шлёт — **guard в `middleware.ts` невозможен**.
      Проверка входа сделана в браузере (`components/admin/AdminGuard.tsx`,
      `GET /admin/auth/me`), там же форма входа: её в макете не было вовсе.
      «Выйти» теперь гасит сессию, а не ведёт на `/login`, как в эталоне.
- [x] **`admin` — обзор.** KPI из `/admin/stats`, живой статус сервисов из `/admin/health`
      (кнопка «Обновить статус» → `?force=1`), недавние регистрации, недавние платежи,
      журнал из `/admin/audit`. Убраны: селектор периода, «Экспорт CSV», все дельты
      («+42 за 7 дней», «+8.2% MoM»), проценты uptime, «p95 124ms» — источников нет.
      ⚠️ Находка: у действий сотрудников `tenant_id` пустой, тенант лежит в `entity_id` —
      без запасного варианта запись о смене тарифа не показывала бы, кому её сменили.
- [x] **`admin/users`.** Поиск с задержкой, чипы тарифов и статусов, таблица, пагинация,
      боковой профиль тенанта, выдача JWT, смена тарифа, блокировка. В модалку смены
      тарифа добавлено **обязательное поле «Причина»** — сервер требует `reason`
      и пишет его в журнал; проверено вживую: без причины `400 REASON_REQUIRED`.
      Из модалки выдачи JWT убран выбор срока — `issue-token` его не принимает.
      Убраны «+ Создать тенант» и «Экспорт CSV»: эндпоинтов нет.
- [x] **`admin/integrations`.** Статистика n8n, поиск тенанта, его воркфлоу с включением
      и выключением, сброс лимита, таблица интеграций всех тенантов. Убраны «Открыть
      n8n UI» (наружу не выставлен) и «Перезапустить worker» (эндпоинта нет).
- [x] **Четыре экрана без макета:** платежи, сборки .epf, очереди/DLQ, обращения по ПДн.
      Свёрстаны по образцу `admin-users.html`. Каждый переживает отказ своего источника
      и объясняет, чего не хватает, — вместо пустой таблицы, которая соврала бы
      «данных нет».
- [x] **Контракты `lib/contracts/admin.ts` и обёртки `lib/api/admin.ts`** — сверены
      с кодом сервисов внутри контейнера, а не с документацией.
      ⚠️ Поймано на этом: `/admin/payments` отдаёт `{payments, count}`, а не массив —
      экран падал с client-side exception, пока не поправил.

### Э4 — Billing: сделано (2026-07-29)

- [x] **`billing`.** Текущий тариф (название, срок, цена), лимиты с прогресс-барами,
      история платежей, способ оплаты, реквизиты плательщика, промо-карточка, документы.
      Всё из `GET /lk/billing`, `/lk/plans`, `/lk/dashboard`, `/lk/profile` — ничего не зашито.
      ⚠️ Срок считается по `valid_until === null`, а не по `days_left` (см. Э2).
- [x] **Убрано из макета:** ссылки «Чек» и «УПД» и «Скачать все →» — сервер документов
      не отдаёт, а УПД у ИП на УСН не выдаётся. Колонки «Документы» в таблице тоже нет.
- [x] **Прогресс-бар «Операций X / Y» показывает только лимит** — фактического счётчика
      операций на сервере нет до промта S10, выдумывать число нельзя. Для интеграций
      и запусков n8n факт есть, там полосы настоящие.
- [x] **Оплата.** `POST /lk/billing/pay`; `payment_url: null` и ошибки шлюза дают понятное
      сообщение, а не пустой экран. ⚠️ Эта ветка нужна **и после** подключения Robokassa.
      Обрабатываются `CUSTOM_PRICE_PLAN` и `PROMO_ALREADY_USED`.
- [x] **«Запросить счёт» — настоящая заявка** через `POST /lk/contact` (`source: "billing"`),
      с номером `REQ-NNNN` в ответе. В макете это была заглушка-попап.
- [x] Написано прямо, что **автопродления нет** (просил Дмитрий): по окончании периода
      доступ прекращается, повторного списания не будет.
- [ ] ⏳ Остаётся до merchant ID: проверить реальный редирект на форму Robokassa и возврат.

### Э3 — Интеграции и воркфлоу: сделано (2026-07-29)

- [x] **`my-integrations`.** Карточки, вкладки по категориям со счётчиками, поиск, фильтр
      по статусу, пауза/возобновление, форма ключей, удаление. Справочник адаптеров
      (имя, категория, цвет, буквы на плашке) вынесен в `lib/adapters.ts` — 34 записи
      из `design-source/assets/integrations-data.js`.
      ⚠️ **Модалка каталога из макета не перенесена, и не будет:** завести интеграцию
      с сайта нельзя. У `/lk/integrations` есть только чтение, пауза, ключи и удаление —
      создаёт её .epf через bridge. Кнопка «+ Добавить интеграцию» ведёт на `/epf`.
      Поля «Запросов за месяц», «Контрагент», «Склад 1С» сервер не отдаёт → «—»
      (вместо них показываю счётчик ошибок, он в контракте есть).
      Три `.preview-block` внизу макета — макеты состояний, а не секции: пустое состояние
      стало условным, остальное не переносится.
- [x] **`workflows`.** Макета нет вовсе — в `shell.js` пункт «n8n-воркфлоу» ведёт на публичную
      `n8n.html`. Собран по образцу соседних экранов: лимит запусков с прогресс-баром
      (`n8n_usage` из `/lk/dashboard`), каталог шаблонов, история запусков.
      На пробном тарифе `limit: 0` — экран честно пишет, что n8n входит в платные тарифы,
      и не даёт нажать «Включить».
      ⚠️ **Каталог воркфлоу на сервере пуст** (`GET /lk/workflows/catalog` → `[]`), как и
      история. Экран это показывает словами, а не пустотой.
- [x] Прогон после переноса: обход обеих страниц — чисто, `npm run test:all` — 70 + 29 зелёных.

### Заглушки .epf и находка по скачиванию (2026-07-29)

- [x] **Положены временные сборки .epf** (решение Дмитрия: «положи временные, потом заменим»).
      Четыре конфигурации, версия `0.0.1`, файлы —
      `/opt/corebridge/epf/<config>/0.0.1/unified_<config>.epf`, строки в `platform.epf_versions`
      с честным `release_notes`: «Временная заглушка… Заменить перед сквозным тестированием».
      ⚠️ Это **не рабочие обработки 1С**, а текстовые файлы. Каталог `epf/` в git не отслеживается
      (`git ls-files epf` пуст) — автодеплой их не затрёт.
      **Заменить перед сквозным прогоном:** положить настоящий файл по тому же пути и обновить
      строку (`sha256`, `sha256_hash`, `file_size`, `version`).
- [x] **Добавлен маршрут `/cdn/` → bridge (3001)** в vhost `corebridge.ru`.
      `GET /lk/epf/download` отдаёт не файл, а одноразовый токен и адрес
      `/cdn/epf/download?token=…`; в vhost такого маршрута не было вовсе, ссылка вела в никуда.
      ⚠️ `proxy_buffering` уже задан в `snippets/proxy-params.conf` — второй раз nginx не примет.
- [x] **Экран `.epf` скачивает правильно:** запрашивает адрес и переходит по нему,
      а не дёргает эндпоинт как файл. `file_size` приходит из Postgres строкой (bigint) —
      приводится явно, иначе в подписи был бы NaN.
- [!] **🔴 Скачивание .epf не работает из-за бага сервера.** `GET /lk/epf/download` отвечает
      `500 REDIS_ERROR`. Причина: `epf_download.service.js` поднимает свой клиент Redis из
      `REDIS_URL`, а там **нет пароля** (`redis://redis:6379`), тогда как рабочий
      `SESSION_REDIS_URL` пароль содержит. В логах — `NOAUTH Authentication required`.
      Сессии живы, поэтому баг и не был заметен. Промт готов:
      **`Documents/prompts/backend_S11_epf_redis.md`** → передать локальному claude-code.
      Экран пока показывает «скачивание временно недоступно», а не молчит.

### Найдено тестовым аккаунтом (2026-07-28)

- [x] **Guard ЛК не пропускал внутрь даже с валидной сессией.** В `middleware.ts` адрес
      проверки собирался как `new URL('/lk/auth/session', req.url)`, а внутри middleware
      `req.url` равен `https://localhost:3005/…` — Next подставляет свой внутренний хост,
      но со схемой `https`, хотя на 3005 слушает обычный `http`. Запрос падал на рукопожатии
      TLS, управление уходило в `catch`, и человека с рабочей сессией уводило на форму входа.
      **Личный кабинет был недоступен целиком.** Баг жил незамеченным, потому что сессий
      до сих пор не существовало, а отказ без cookie работал исправно — тесты проходили.
      Исправлено: адрес задаётся явно (`LK_API_INTERNAL`, по умолчанию `http://127.0.0.1:3000`),
      имя домена передаётся заголовком. Добавлен e2e-набор «Guard пропускает с сессией»
      (пропускается, если нет `CB_SESSION`).

### Э1 — Auth: сделано (2026-07-28)

- [x] **Общая основа экранов.** `styles/auth.css` — стили из `<style>` пяти макетов, сведённые
      в один файл (подключён импортом в `app/(site)/(auth)/layout.tsx`, а не глобально:
      имена `.tabs-nav`, `.divider` встречаются и в других макетах со своими значениями).
      ⚠️ В макете обе раскладки звали карточку одним классом `.auth-card` — здесь вариант
      «по центру» вынесен под `.auth-center`, иначе правила перебивали друг друга.
      Компоненты: `AuthSplit` / `AuthCenter` / `AuthTabs`, `PasswordField` с глазом,
      `PasswordStrength`, `Alert`, `YandexButton`, `TwoFactorStep`.
      Слой API: `lib/api/auth.ts`, типы `lib/contracts/auth.ts`.
- [x] **`login`.** Пароль, Яндекс ID, **вход по ссылке из письма** (в макете кнопки не было),
      двухшаговый вход при 2FA с показом `attempts_left`, разбор `?error=` от `middleware.ts`,
      возврат на `?next=`. Убраны: кнопка Google, отзыв «Игорь П., Ритейл-М», чекбокс
      «Запомнить меня» (у `POST /lk/auth/login` такого параметра нет — переключатель,
      который ничего не делает, хуже, чем его отсутствие). Расхождение с макетом **4,0 %**.
- [x] **`register`.** Тексты приведены к реальному тарифу: бессрочный пробный, 500 операций
      в месяц. Убрано «Уже с нами: 1 247 компаний». Проверка совпадения паролей и согласия
      с офертой до отправки. Расхождение **5,0 %**.
- [x] **`forgot-password`.** Два состояния макета стали шагами одной формы; блок `.variants`
      из макета — это макет состояния, а не секция, вместе они не выводятся. Расхождение **3,3 %**.
- [x] **`reset-password`.** Токен из `?token=`; при включённой 2FA сервер меняет пароль,
      но сессию не выдаёт — ведём на тот же экран кода, что и при входе. Расхождение **3,3 %**.
- [x] **`verify-email`.** Подтверждение по ссылке + состояние ожидания письма; таймер
      повторной отправки честно отсчитывает (в макете он был статичный). Расхождение **0,5 %**.
- [x] **`invite/accept`** — макета не было, сверстан по образцу остальных экранов.
- [x] **Починена ссылка из письма-приглашения.** `${LK_BASE_URL}/lk/invite/accept?token=…`
      отдавала **404**: весь `/lk/*` nginx проксирует в API, а такого маршрута там нет.
      Закрыто на своей стороне, без правок бэкенда: точное совпадение
      `location = /lk/invite/accept` → 3005 в `deploy/nginx/corebridge.ru.conf` плюс
      переписывание пути в `middleware.ts`. Проверено: оба адреса отдают 200.
- [x] **Уточнён контракт входа.** Сервер отвечает **`INVALID_CREDENTIALS`** (401), а не
      `UNAUTHORIZED`, как можно было понять по сводке S1. Проверено запросом к живому API,
      коды дописаны в `ERR`. Там же: `TOKEN_INVALID`, `TOKEN_EXPIRED`, `WEAK_PASSWORD`
      (с `min_length`), `EMAIL_EXISTS`, `ALREADY_VERIFIED`.
- [x] **Найдена и исправлена горизонтальная прокрутка** на регистрации при ширине 390px:
      у `.btn` в `site.css` стоит `white-space: nowrap`, и подпись «Зарегистрироваться через
      Яндекс ID» (длиннее гугловской из макета) распирала колонку на 7px. Нашёл `inspect.mjs`.
- [x] **Тесты:** `tests/e2e/auth.spec.ts` — 19 проверок механики против живого API;
      пять экранов переведены в `ported: true`, попиксельный гейт по ним теперь работает.
      Весь прогон: **64 зелёных**.

### Прочее

- [x] **Э0.4 `sitemap.xml` + `robots.txt`** (2026-07-28). `app/sitemap.ts` — 10 публичных
      страниц; формы входа и регистрации в карту не включены (содержания нет, в выдаче мешают).
      `app/robots.ts` закрывает экраны кабинета, `/lk/`, `/admin`, `/api/` и указывает на карту.
      ⚠️ Отдельно закрыт **админ-субдомен**: Next.js на 3005 общий с основным доменом и отдавал
      оттуда `robots.txt` с «Allow: /». Строгий `Disallow: /` отдаёт nginx —
      `location = /robots.txt` в `deploy/nginx/admin.corebridge.ru.conf`.
      Проверено вживую и тремя e2e-тестами.
- [x] **Э0.3 Тесты + инструменты самопроверки** (2026-07-28).
      **Vitest** — 29 тестов: разбор ответов и ошибок `lib/api/client.ts` (коды из тела,
      детали вроде `attempts_left`, `204` без тела, не-JSON от nginx), `initialsOf`,
      разметка `Sidebar` и публичных шапки/подвала (держат три исправленных бага макета).
      **Playwright на Firefox** — 36 тестов: 15 публичных страниц отдают 200 и реально
      применяют `site.css`, guard ЛК уводит на `/login?next=…` по всем шести маршрутам,
      `corebridge.ru/admin*` → 404, три экрана админ-субдомена рендерят оболочку,
      `/admin/health` остаётся за API, нигде не осталось ссылок на `.html`.
      **`tools/inspect.mjs`** — обход сайта: скриншоты десктоп + мобильный, ошибки консоли
      и JS, упавшие запросы, ответы 4xx/5xx, битые внутренние ссылки, горизонтальная
      прокрутка на телефоне, безопасное прокликивание попапов и вкладок со снимком после
      каждого нажатия. Отчёт — `artifacts/report.md`.
      **`tools/compare.mjs` + `tools/design-server.mjs`** — эталон раздаётся на 3006,
      снимки макета и сайта сводятся `pixelmatch`, разница пишется в `diff.png`.
      Гейт `tests/visual/compare.spec.ts` держит только страницы с `ported: true`
      в `tests/visual/pages.json` — заглушки пропускаются, а не краснеют.
      **Карта `tests/visual/pages.json`** — 26 страниц: маршрут, макет, нужна ли сессия,
      допуск расхождения и что помнить при сверке. Заодно зафиксировано, что макетов
      нет у `/workflows` (в `shell.js` пункт «n8n-воркфлоу» ведёт на публичную `n8n.html`)
      и у `/invite/accept`.
      Прогон всех 26 страниц — чисто; закрытые экраны показывают вход, как и должны.
- [x] **Найден и закрыт баг: CSP резала шрифты** (2026-07-28, нашёл первый же обход).
      nginx отдаёт `style-src 'self' 'unsafe-inline'; font-src 'self' data:`, а `layout.tsx`
      тянул Inter с `fonts.googleapis.com` — браузер молча блокировал, и **весь сайт
      рисовался системным шрифтом** вместо Inter. В консоли это было видно, глазами почти нет.
      Решение: Inter и JetBrains Mono выложены у себя — `public/assets/fonts.css` +
      8 вариативных `woff2` (латиница и кириллица, 244 КБ). CSP не трогали: сервис для
      российских клиентов не должен зависеть от доступности Google. Держат три e2e-теста.
- [x] **Э0.1 `middleware.ts`** (2026-07-28). Разводка субдоменов + guard ЛК.
      Проверено на проде:
      · ЛК без сессии → `307 /login?next=…` (все 6 маршрутов, `next` сохраняет цель);
      · публичные `/`, `/pricing`, `/login`, `/oferta`, `/integrations`, `/n8n` → 200;
      · `corebridge.ru/admin*` → **404** (админка с основного домена закрыта);
      · `admin.corebridge.ru/`, `/users`, `/integrations` → 200, экран реально отдаётся;
      · `admin.corebridge.ru/admin/health` → 403 от IP-whitelist, middleware его **не перехватывает**.
      **Коллизия путей админки закрыта.**
      Обработка 403: код из тела (`TENANT_BLOCKED` / `TENANT_PENDING_DELETION`) пробрасывается
      в `/login?error=…` — экраны разные. Если API недоступен, ведём на `/login`, а не показываем
      сломанный дашборд.

---

### Э9 — Сквозной прогон: сделано (2026-07-30)

- [x] **Сверка с макетом стала осмысленной.** Раньше считалась доля по всей
      странице — она ломается от одного переписанного абзаца: на лендинге дала
      15,5 % при полностью совпадающей вёрстке (страница на 328px выше, дальше
      сравнивались разные секции). Теперь считается ещё и первый экран, он от
      длины текста ниже не зависит. По нему: лендинг 0,1 %, оферта 0,1 %,
      каталог 1,2 %. **23 страницы отмечены перенесёнными, гейт зелёный**,
      у каждой допуск по измерению и запись, из чего расхождение складывается.
- [x] **Полный обход с прокликиванием на трёх ширинах** — 30 страниц, чисто.
- [x] **`tools/journey.mjs` — сквозной путь нового клиента.** Заводит настоящий
      аккаунт, идёт с лендинга в регистрацию, проверяет тариф и бессрочность
      лицензии в базе, открывает кабинет, забирает `.epf` (включая одноразовость
      токена), упирается в ограничения пробного тарифа, выходит и вычищает
      за собой. 20 проверок.
- [x] **`tools/icons.mjs` и `tools/usecases.mjs`** — см. раздел о локальном
      тестировании выше.
- [x] **Чистка тестовых данных** — раздел 0.
- [x] **Гейт сверки исправлен.** Он проверял долю по всей странице — то самое
      число, которое ломается от переписанного абзаца. Теперь держит первый экран.
      Заодно: закрытые экраны без cookie показывали форму входа и валили сверку
      красным, хотя смысл был «нет доступа», а не «вёрстка съехала». Теперь
      пропускаются с объяснением — тестовый аккаунт после сдачи вычищен,
      и это обычное состояние, а не оплошность.
      Полный прогон: **29 модульных + 69 браузерных, 0 падений**.

**Оба дефекта сервер закрыл (ответ `site_S14_S15_RESPONSE.md`), проверено 2026-07-31:**

- [x] **S15 — удаление аккаунта доходит до конца.** Сервер не выбрал ни один
      из трёх предложенных вариантов, и был прав: `lk_user:<uuid>` — псевдоним,
      а не персональные данные, поэтому замена его на `deleted_user:<uuid>` ничего
      не давала и только ломалась об триггер. Обезличивание `actor` убрано целиком,
      триггер неизменяемости остался в полном объёме. Настоящая утечка была
      в `new_value` — закрыта на записи в шести местах и вычищена задним числом
      миграцией `027`.
      **Проверено:** в 106 записях журнала ноль почт, телефонов и названий компаний;
      `UPDATE audit_log` по-прежнему отвергается; сквозной прогон проходит чистку
      штатной механикой без обходов.
- [x] **S14 — переигрывание DLQ разведено по происхождению события.**
      `409 NOT_REPLAYABLE` вместо 502, `skipped_ids` отдельно от `failed_ids`.
      Временный список неповторяемых типов на сайте снят — он был обходом на время,
      пока «нельзя» нельзя было отличить от «сломалось». Сайт различает способ
      переигрывания (`webhook` / `tenant_init`) и сообщает об этом по-разному.
      ⚠️ Сервер сверх промта нашёл, что **удаление из очереди тоже не работало** —
      писало в несуществующие колонки. Я написал, что оно работает: проверил
      только листинг.
- [x] **Ночной cron переключён** на `src/scripts/purge_expired.js` внутри образа.
      Прежняя обёртка возвращала ноль, даже если часть тенантов вычистить
      не удалось — из-за этого дефект S15 месяц был невидим. Новый скрипт отдаёт
      `failed` и завершается с кодом 1.
- [x] **Лимит на выкладку `.epf` продублирован в моём vhost.** Сервер поднял его
      до 60 МБ у себя; у меня на `corebridge.ru` действовал общий `10m`. Сегодня
      сборки 1,2–1,3 МБ и хватало, но при первой крупной публикация упиралась бы
      в 413 на одном домене и работала на другом. Проверено файлом 12 МБ.
      ⚠️ `proxy_read_timeout` в этот блок не ставил: он уже есть в `proxy-params.conf`,
      а дубль директивы валит `nginx -t` — сервер на этом обжёгся.

## 5. Следующая задача

**Закрыты все этапы: Э0–Э9.** Сайт собран целиком — публичные страницы, личный
кабинет, админка, юридические документы, поддержка. Прогон пройден, тестовые
данные вычищены.

### Что осталось, и это не код

1. ✅ **Вход и почта — S18 закрыт сервером 2026-07-31, проверено на проде.**

   Cookie переведена на `sameSite: 'lax'` + `Secure`, письма пошли впервые
   за всё время. Мой временный обход снят — переход с чужого сайта ведёт
   прямо в кабинет, guard не вмешивается.

   ```
   cookie: sameSite=Lax, secure=true, httpOnly=true
   регистрация → email_sent, Postfix: status=sent (250 2.0.0 OK)
   вход по ссылке из письма → сразу в кабинете
   приглашение в команду    → письмо ушло, ссылка открывает форму приёма
   ```

   Сервер нашёл то, что мой разбор не задел: те же настройки лежали **второй
   копией** — cookie в `users.routes.js` (приём приглашения), отправка почты
   в `user.service.js` (свой транспорт, игнорировавший оба переключателя TLS).
   Правка одних только найденных мной мест починила бы половину, а приглашения
   продолжили бы падать с «уже починенной» ошибкой сертификата. Обе настройки
   вынесены в общие модули. Флаг `Secure` отвязан от `NODE_ENV`.

   Из истории на сайте осталось полезное: шапка публичных страниц знает про
   вход и показывает «Личный кабинет» (`lib/auth/session-probe.ts`).

   ⚠️ Заодно выяснилось про тестирование: регистрация ограничена **8 попытками
   с адреса**, дальше `429` на несколько минут. Плотный прогон проверок в это
   упирается — не поломка, а защита.

<details>
<summary>Как это выглядело (сохранено — история диагностики)</summary>

   Разбор жалобы «новый аккаунт на сайте не создаётся» (31.07). Жалоба верна
   по ощущениям и неверна по факту: **аккаунты создаются**, в базе лежит
   `eralonecb@yandex.ru` с отметкой ровно на момент возврата из Яндекса.

   **а) Cookie сессии стоит с `SameSite=Strict`** и потому не отправляется
   в переходе с чужого сайта — из письма, из мессенджера, с `oauth.yandex.ru`.
   Cookie при этом жива; guard видит запрос без неё и уводит на форму входа,
   а обновление страницы не помогает — обновляется уже форма входа.
   Отсюда обе жалобы сразу: и «аккаунт не создаётся», и «вход держится только
   в одном окне». Проверено при одной и той же живой сессии:

   | | было | стало |
   |---|---|---|
   | адресная строка, закладка | кабинет | кабинет |
   | ссылка с чужого сайта в кабинет | форма входа | кабинет |
   | обновить страницу после этого | форма входа | кабинет |

   Лечение — `sameSite: 'lax'`: межсайтовые POST по-прежнему без cookie,
   то есть защита от CSRF на месте.

   **б) Почта не уходила ни разу.** `SMTP_HOST=172.21.0.1`, а сертификат
   Postfix выписан на `corebridge.ru` — nodemailer рвёт STARTTLS. Успешных
   отправок за всё время: **0**. Отсюда же `verification_sent: false` в ответе
   регистрации. Лечится строкой в `.env` — `SMTP_TLS_INSECURE=true`, код
   этот переключатель уже поддерживает.

   **в) На проде `NODE_ENV=development`** у всех четырёх сервисов, из-за чего
   сессионная cookie идёт без флага `Secure`.

   **Что сделано у меня:** временный обход в `lib/auth/session-probe.ts`.
   Ограничение `Strict` касается переходов, но не запросов со страницы:
   `fetch` к своему же серверу считается своим. Поэтому форма входа не гадает,
   а спрашивает `GET /lk/auth/session`, и при живой сессии возвращает человека
   туда, куда он шёл. Возвращение одно на вкладку — иначе заблокированный
   тенант (guard отказывает, а сессия жива) попал бы в петлю; случай `?error=`
   пропускается отдельно. **После `lax` удалить.**

   Заодно: шапка публичных страниц теперь знает про вход и показывает
   «Личный кабинет». В эталоне она только гостевая — вошедший видел «Войти»
   и не имел пути обратно в кабинет, что усиливало ту же самую жалобу.
   Ответ спрашивается один раз на вкладку и сбрасывается входом и выходом,
   чтобы лендинг не дёргал `lk-api` на каждой странице.

   И форма регистрации теперь подводит к сообщению об ошибке и ставит курсор
   на непринятое согласие — раньше нажатие внизу длинной формы выглядело
   как «ничего не произошло».

</details>

2. ✅ **Настоящие `.epf` выложены** (31.07). Все четыре конфигурации, `v1.0.0`,
   размеры и контрольные суммы сошлись с таблицей сервера; заглушки `0.0.1`
   сняты с активации. Скачивание проверено сквозным прогоном — клиент получает
   именно сборку, повторное использование токена даёт 410.

   Путь занял три захода, и все три причины были разные:
   - том `epf` монтировался в bridge `:ro` (**S16**) — правка приехала деплоем,
     моё временное наложение compose снято;
   - активация звала `platform.activate_epf_version` с чужой сигнатурой
     (**S17**). Причина оказалась глубже, чем я думал: перегрузка `(text,text)`
     существовала в репозитории, но **никогда не накатывалась** — её дописали
     в уже применённый файл миграции `011`, а менеджер сверял только номер.
     Сервер добавил сверку sha содержимого миграций, чтобы такое не повторялось;
   - права каталога (`o+x` для nginx) держались моей ручной командой — забраны
     в `deploy.sh`.

   ⚠️ Публикацию по-прежнему делает воркфлоу в `corebridge-epf`; чтобы он
   ходил сам, там нужен секрет `COREBRIDGE_PUBLISH_TOKEN`.

3. **Robokassa.** Merchant ID выдают после готового сайта — он готов. После
   подключения проверить реальный редирект на форму оплаты и возврат.
4. **Включить TOTP у `admin@corebridge.ru`.** Панель меняет тарифы, блокирует
   клиентов и запускает удаление аккаунтов; сейчас вход защищён только паролем
   и списком IP. Код второго фактора готов.
5. **Решить вопрос по ст. 18.5 152-ФЗ** — данные лежат в ЕС. Формулировку
   в политике я привёл к трансграничной передаче по ст. 12, но фактическое
   соответствие требует либо переноса БД в РФ, либо юридической консультации.
   Вынесено в `questions_open.md`.

---

**Ниже — уже закрытое.**

### [~] Э8 — Статика, SEO, юридические страницы, прайс ← **в работе**

- [x] **Документация по .epf — раздел из 33 инструкций (2026-08-01).**

      Исходники команды модуля (`~/Downloads/CoreBridge_Инструкции`) перенесены
      в репозиторий как `content/epf-docs/` + 69 скриншотов в `public/docs/images/`
      и собираются в HTML скриптом `tools/build-docs.mjs` (`npm run docs`;
      он же встроен в `npm run build` — манифест обязан идти в ногу со страницами).

      Что делает сборщик, кроме разбора markdown: переписывает перекрёстные ссылки
      и пути картинок под маршруты сайта, держит якоря заголовков в стиле GitHub
      (исходники ссылаются друг на друга якорями, и они должны продолжать работать),
      подставляет размеры PNG из IHDR против скачков вёрстки, **падает на битой
      ссылке, потерянном якоре и неизвестной картинке**.

      Адреса и короткие имена в навигации заданы руками в `SECTIONS`, а не
      транслитерацией: адрес — часть публичного контракта, а «Как настроить заказы
      и отгрузку», срезанное регуляркой, даёт винительный падеж в оглавлении.

      Страницы: `/docs/epf` (оглавление + сводные таблицы из README исходников)
      и `/docs/epf/<slug>` — 33 статики. Своей вёрстки почти нет: раскладка взята
      у `docs.html`, добавлены карточки, тело статьи и раскрывающаяся навигация
      для телефона (`.docs-side` до 820px скрыт, без неё из статьи некуда уйти).

- [x] **Навигация документации приведена к одному виду (2026-08-01, по замечанию Дмитрия).**

      Левая панель — `components/DocsNav.tsx` — теперь одна на всю документацию,
      включая `/docs`: все 33 инструкции по семи группам, группа сворачивается
      (`<details>`, без JavaScript), у каждой счётчик. Раскрыта та группа, где
      лежит открытая страница; сама страница отмечена подсветкой, полосой слева
      и `aria-current="page"`. Прежний список якорей `/docs` из левой панели убран:
      он дублировал правую, где ему и место.

      Правая панель везде показывает заголовки текущей страницы. На `/docs`
      к ней добавлены пять шагов пути до токена (`#token-1…5`) вложенным уровнем —
      к ним возвращаются чаще всего.

      Сверено по живому сайту на всех 35 страницах раздела: **280 якорей правой
      панели — битых нет, 35 ссылок левой — все 200, отметка текущей страницы
      верна везде, группа с открытой страницей всегда раскрыта.**

- [x] **Поисковая оптимизация всех страниц (2026-08-01, семантическое ядро от Дмитрия).**

      Ядро: `~/Downloads/CoreBridge_filtered (расширение под категории).csv`,
      500 фраз в 12 категориях.

      ⚠️ **Половина ядра к продукту не относится, и внедрять её нельзя.**
      Самая частотная группа — «карточки для маркетплейсов» (42 979/мес),
      «нейросеть для карточек», «дизайнер карточек» — это про оформление
      карточек товара, услуга другого рынка. Туда же «1с элемент», «м видео
      маркетплейс», «интеграция ЕНС в 1с». Под такие запросы пришлось бы писать
      о том, чего продукт не делает: трафик придёт и уйдёт, а страницы получат
      отказы. Не берём. Отдельно: спрос на ЭДО (Диадок, СБИС, Контур) заметный,
      но документооборота в модуле нет — этих слов на сайте тоже нет.

      Взят релевантный пласт: «интеграция 1с», «интеграция с маркетплейсами 1с»,
      «синхронизация 1с ут», «интеграция 1с с сайтом», «интеграция битрикс24
      и 1с», «интеграция 1с и сдэк», «эквайринг в 1с», «интеграция 1с телеграм»,
      «честный знак в 1с ут», «выгрузка данных 1с в power bi», «1с api».

      Что сделано:

      · **заголовки и описания** переписаны на всех публичных страницах и на 22
        из 33 инструкций (карта `SEO` в `build-docs.mjs`; у остальных заголовок
        инструкции и первый абзац — они отвечают на запрос и так). Длина
        выдержана: title ≤ 62, description 70–175;
      · **описание из первого абзаца** теперь режется по границе предложения,
        а не по счётчику символов — раньше обрывалось на полуслове;
      · **Open Graph и Twitter-карточка** + `public/og.png` 1200×630, рисует
        `tools/make-og.mjs` фирменными шрифтом и цветами. Раньше ссылка на сайт
        в Telegram и ВКонтакте выглядела голой строкой;
      · **фавиконка** `app/icon.svg` — её не было вовсе;
      · **структурированные данные**: Organization, WebSite и SoftwareApplication
        на главной, BreadcrumbList и TechArticle на страницах документации.
        Цен, рейтингов и FAQPage сознательно нет: цены приходят из API и меняются,
        отзывов у нас не существует, а ответы в тарифах собираются в браузере —
        разметка разошлась бы с текстом;
      · **карта сайта** берёт дату изменения из истории git по конкретному файлу.
        Раньше каждая выкладка объявляла изменившимися все страницы разом;
      · `robots.txt` уже был (`app/robots.ts`) и закрывает кабинет правильно —
        оставлен как есть. ⚠️ `Disallow: /epf` не задевает `/docs/epf`:
        сопоставление префиксное от начала пути.

      **Найдено и исправлено по ходу:** на `/n8n` не было ни одного `<h1>`
      (баннер вёрстан вторым уровнем ещё в эталоне) — для поиска это страница
      без названия. На `/docs` и `/docs/epf` было по два `<h1>` — шапка раздела
      и заголовок текстовой колонки спорили, чему посвящена страница. Вид
      в обоих случаях не изменился, размеры заданы классами.

      Проверка: `npm run seo` (`tools/seo-audit.mjs`) обходит все 41 публичную
      страницу и смотрит title, description, canonical, og:image, единственность
      `<h1>`, уникальность заголовков и разбираемость JSON-LD — **0 замечаний**.
      Выборка из тех же проверок закреплена в `tests/e2e/seo.spec.ts` — она
      разбирает ответ сервера, а не открывает страницы браузером: заход тянет
      стили, шрифты и скрипты, и семь заходов снова подводили набор под лимит.

- [x] **Полный прогон стабилизирован: `workers: 2` (2026-08-01).**

      Набор месяцами краснел в случайных местах — то `/register`, то `/pricing`,
      то `/verify-email`, то чужой тест про субдомены. Каждый раз это оказывалась
      страница nginx **«429 Too Many Requests»**, а не поломка: одна страница
      тянет 10–20 запросов, четыре воркера по живому проду регулярно выбирали
      `per_ip` 30 r/s. Свою долю добавляли и мои проверки — их пришлось перевести
      на файлы и HTTP (см. выше), но одного этого не хватило.

      С двумя воркерами прогон зелёный целиком: **101 passed, 25 skipped,
      0 failed**. Цена — полторы минуты: 1.8 мин против 2.5 мин.

- [x] **Первичная настройка и перекрёстные ссылки (2026-08-01, второе замечание Дмитрия).**

      `/docs` в левой панели переименована из «Токен и API платформы»
      в **«Первичная настройка»** и стала **первым пунктом группы «Установка»**:
      это то, с чего установка начинается, а прежнее имя ставило справочник API
      вперёд задачи.

      **Перекрёстные ссылки добавлены правилами сборки, а не правкой markdown.**
      `content/epf-docs/` — поставка команды модуля, следующая перезапишет файлы
      и ручные правки молча исчезнут. В `build-docs.mjs` появились `CROSSREFS`
      (по образцу `build-legal.mjs`: у каждого правила `why` и ожидаемое число
      срабатываний, расхождение — ошибка сборки) и карта `MECHANIC_LINKS`.
      Итого 44 ссылки:

      · «JWT-токен — берётся в Личном кабинете» во всех четырёх инструкциях
        по установке → «[как именно](/docs#token)»;
      · раздел «2. Получение файла» → врезка «Тот же путь по шагам, со
        скриншотами каждого экрана кабинета»;
      · Шаг 1 мастера подключения → «[где его взять](/docs#token-4)»;
      · названия механик в таблицах особенностей конфигураций («Маркировка
        «Честный знак»», «FBM/FBO», «Финансовые отчёты МП», «Типы цен»,
        «Резервирование», «Учёт ГТД» и прочие) → на свои инструкции. Ссылка
        не ставится, если в той же строке она уже есть.

      Поведение закреплено тестами (19 в `docs-epf.spec.ts`).

- [x] **Путь «вход → JWT-токен» на `/docs` со скриншотами кабинета.**

      Пять шагов: вход, «Файл .epf» в меню, выбор конфигурации, копирование токена,
      скачивание файла. Снимки делает `tools/docs-shots.mjs` — заводит временный
      аккаунт на проде, подсвечивает, куда нажимать, и **вычищает аккаунт тем же
      порядком, что `journey.mjs`**. Токен на снимках подменён образцом, почта —
      на `client@example.ru`; остальное настоящее.

      Старые «Шаг 1. Скачать .epf» и «Шаг 3. Вставить токен» новый раздел покрыл
      целиком — вместо них теперь «Установка модуля в 1С» с карточками по четырём
      конфигурациям и «Первое подключение» со ссылкой на мастер.

      ⚠️ **Найдено съёмкой, исправлено:** сервер отдаёт версию уже с буквой
      (`"version": "v1.0.0"`), а экран `/epf` подставлял свою — на кнопке было
      «vv1.0.0», файл скачивался как `CoreBridge_UT11_vv1.0.0.epf`. Там же
      «Для Управление торговлей 11»: предлог требует родительного падежа,
      а названия конфигураций лежат в именительном. Нормализация версии — `ver()`,
      предлог убран. Кнопка «Инструкция по установке» теперь ведёт в инструкцию
      под выбранную конфигурацию, а не в общее оглавление.

      ⚠️ **Про тесты раздела** (`tests/e2e/docs-epf.spec.ts`). Первая версия
      проверяла по HTTP каждую из 33 страниц и каждую из 69 картинок — 120
      запросов сверх набора. nginx держит на сайте `per_ip` 30 r/s с запасом 100,
      воркеры Playwright делят лимит на всех, и полный прогон начал ронять 429
      куда попало: то `/docs`, то `/verify-email`, то чужой тест про субдомены.
      Доказательство — скриншот упавшего теста: страница nginx «429 Too Many
      Requests». Сплошные проверки переведены на файлы (картинки — статика
      в `public/`, маршруты строит `generateStaticParams` из того же манифеста,
      битые ссылки и якоря ловит сборщик), по HTTP осталась выборка из четырёх
      адресов. Плюс в браузерных тестах раздела заблокирована загрузка картинок
      (`page.route(... route.abort())`): инструкция тянет до 14 скриншотов,
      семь заходов — сотня запросов, а проверяется в них разметка.

      ⚠️⚠️ **Главная находка, и она не тестовая: предзагрузка Next.js упиралась
      в лимит nginx.** Разбор 429 в `access.log` показал, что отказы прилетают
      на запросы с `?_rsc=` — это `<Link>`, подгружающий содержимое каждой
      ссылки в поле зрения. На оглавлении раздела 33 карточки плюс столько же
      в навигации (список рисуется дважды — колонкой и списком для телефона),
      то есть **66 запросов одним всплеском на каждое открытие страницы**, при
      `per_ip` 30 r/s. Получал их и живой посетитель, не только тесты.
      Исправлено: `prefetch={false}` на карточках и во всей навигации раздела.
      Замер: прирост 429 в логе за два полных прогона набора — **13 вместо
      сотен**. Страницы статические, на скорость перехода это не влияет.

      ⚠️ **Что осталось и почему это не про документацию.** После исправления
      оставшиеся 429 приходят только на служебные запросы API: `/lk/auth/session`
      (24 из последних 40 — шапка проверяет сессию на **каждой** публичной
      странице, а набор открывает их сотнями), `/lk/plans`, `/admin/auth/me`,
      `/admin/health`. Отсюда единственный оставшийся красный тест полного
      прогона — «/admin/* на субдомене остаётся за API»: он ждёт 403 от
      IP-whitelist, а под нагрузкой получает 429. В одиночку проходит всегда
      (проверено трижды), `curl` к `/admin/health` отдаёт 403, как и положено.
      Мои тесты этот хост не трогают вовсе. Итог последнего прогона:
      86 passed, 25 skipped, 1 failed — этот самый.

      ⚠️ **Побочно найдено:** тест `auth.spec.ts` «ответ одинаков для любого
      адреса» слал фиксированный `nobody-here@example.com`. Запросы сброса
      ограничены **и по адресу**, а не только по IP, как записано
      в `site_server_integration_reference.md` («5 попыток / 15 мин на IP»):
      другой адрес с того же IP получает 202 мгновенно, а исчерпанный отвечает
      429 и через 16 минут. Тест переведён на уникальный адрес.

- [x] **Путь «вход → JWT-токен» на `/docs` со скриншотами кабинета.**

      Пять шагов: вход, «Файл .epf» в меню, выбор конфигурации, копирование токена,
      скачивание файла. Снимки делает `tools/docs-shots.mjs` — заводит временный
      аккаунт на проде, подсвечивает, куда нажимать, и **вычищает аккаунт тем же
      порядком, что `journey.mjs`**. Токен на снимках подменён образцом, почта —
      на `client@example.ru`; остальное настоящее.

      Старые «Шаг 1. Скачать .epf» и «Шаг 3. Вставить токен» новый раздел покрыл
      целиком — вместо них теперь «Установка модуля в 1С» с карточками по четырём
      конфигурациям и «Первое подключение» со ссылкой на мастер.

      ⚠️ **Найдено съёмкой, исправлено:** сервер отдаёт версию уже с буквой
      (`"version": "v1.0.0"`), а экран `/epf` подставлял свою — на кнопке было
      «vv1.0.0», файл скачивался как `CoreBridge_UT11_vv1.0.0.epf`. Там же
      «Для Управление торговлей 11»: предлог требует родительного падежа,
      а названия конфигураций лежат в именительном. Нормализация версии — `ver()`,
      предлог убран. Кнопка «Инструкция по установке» теперь ведёт в инструкцию
      под выбранную конфигурацию, а не в общее оглавление.

      ⚠️ **Найдено частыми прогонами набора:** тест «ответ одинаков для любого
      адреса» слал фиксированный `nobody-here@example.com` и после нескольких
      прогонов сам себя блокировал. Причина не та, что записана в
      `site_server_integration_reference.md` §«лимит 5 попыток / 15 мин на IP`:
      сервер считает запросы сброса **по адресу**, а не по IP — другой адрес
      с того же IP получает 202 мгновенно, а исчерпанный отвечает 429 и через
      16 минут. Тест переведён на уникальный адрес; на продукт это не влияет,
      но справочник по этому пункту врёт.


- [x] **Лендинг (`/`) собран 2026-07-29.** Все секции эталона: hero, 33 сервиса
      в 8 категориях, «Почему CoreBridge», «Как начать», тарифы, призыв, слово
      основателя. Правки против макета:
      · `1С:ERP` → `1С:БП` в бейджах hero и в тексте про конфигурации: сервер
        собирает `.epf` для `ut11|unf|ka|bp`, ERP не существует;
      · тарифы и цены — из `GET /lk/plans`, не зашиты;
      · описание пробного тарифа исправлено: в макете «до 5 операций в сутки,
        30 дней», на сервере — **бессрочный**, 500 операций в месяц, 1 интеграция;
      · «33 сервиса в 8 категориях» считается по справочнику адаптеров, а не написано
        руками («Иное» и «нужен другой» в счёт сервисов не идут);
      · пустой пункт меню на `about.html` убран — страницы не существует.
- [x] **Шапка получила мобильную версию.** В эталоне её не было: пять пунктов меню
      и две кнопки стоят в строку без переноса, из-за чего на 820px страница
      вылезала за экран на 234px, а на 390px — на 664px. Поймано новой проверкой
      на трёх ширинах. До 900px меню уезжает в раскрывающуюся панель.
- [x] **`pricing`.** Карточки, таблица сравнения и FAQ целиком из `GET /lk/plans`.
      Пустая последняя строка таблицы не переносится (артефакт редактирования макета).
      Кнопки оплаты различают гостя и вошедшего: гостю платить нечем, тенанта ещё нет.
      ⚠️ Четыре ответа FAQ переписаны — макет обещал приостановку операций при
      превышении лимита (он мягкий), пропорциональный пересчёт при смене тарифа
      (его нет) и отправлял за n8n UI на «Энтерпрайз», хотя он есть и на
      «Профессионале» — том самом, что продаётся по промо.
      ⚠️ Найдено при сборке: «Без лимита» в таблице нельзя определять по величине
      числа. С порогом 99 999 «Профессионал» получал «Без лимита» на операциях
      (у него 100 000) и выглядел равным старшему тарифу. Определяется по тарифу.
- [x] **Юридические страницы.** Текст вынесен в `content/legal/`, готовит
      `tools/build-legal.mjs` из эталона по правилам с основаниями. Скрипт падает,
      если правка перестала находить своё место, — дословность документов не зависит
      от ручного переноса 50 КБ текста. Применено: Нидерланды → Германия,
      локализация по ст. 18.1 → трансграничная передача по ст. 12, пробный тариф
      бессрочный с месячным лимитом, приостановки операций нет, адрес убран из оферты.
- [x] **`contacts`.** Убраны реквизиты несуществующего ООО «КорБридж» с ИНН, КПП,
      ОГРН и гендиректором, офис «в БЦ Skyline», карта, схема проезда и Telegram
      с «ответом ~4 мин». Осталось: почта, телефон из оферты, три поля для
      идентификации исполнителя. Форма → `POST /lk/contact`.
- [x] **`for-business`.** Убраны кейсы с отзывами от имени названных людей, прайс
      на услуги, «SLA 99.95 %», «24/7», «реагирование за 15 минут», «инженеры
      подключат за 2–6 недель», агентская программа, white-label, «CoreBridge Partner».
- [x] **`integrations`.** 33 сервиса в 8 категориях, фильтр, счётчики по справочнику.
      Карточка перестала быть кнопкой: в макете она открывала модалку подключения,
      а на публичной странице подключать нечего.
- [x] **`docs`.** Только API типа 1, все маршруты проверены живым запросом
      с настоящим токеном. «Шаг 3» переписан под JWT — ключей `cb_live_*` не существует.
      Выдуманные `/workflows/{id}/run` и `/usage` убраны, поиск по документации убран.
      ⚠️ База — `api.corebridge.ru`: на `corebridge.ru` префикс `/api/v1/` не проксируется.
- [x] **`n8n`.** ⚠️ В макете это был экран **внутри кабинета** с замком
      «разблокируется на тарифе», а по карте маршрутов `/n8n` публичный. Замок,
      оверлей и вкладки убраны: сюда приходят из поиска, ещё не будучи клиентом.
      Макет к тому же противоречил сам себе — баннер обещал «Профессионал»,
      текст ниже «Энтерпрайз». Название тарифа берётся из каталога.
- [x] **Полный прогон на трёх ширинах (2026-07-30):** 30 страниц, включая ЛК
      и админку. Все чисто. Нашлись и исправлены три места, где эталон ломался
      на узких экранах:
      · шапка публичных страниц не имела мобильной версии вовсе — уносила страницу
        за экран на 234px на планшете и 664px на телефоне;
      · `.docs-layout` — колонка грида `1fr` не сжимается уже своего содержимого,
        а внутри таблица с `min-width: 560px`. Нужен `minmax(0, 1fr)`;
      · `ul.legal-list li` — флекс-контейнер, текст внутри становится анонимным
        флекс-элементом с `min-width: auto`. Пункт с адресом почты растягивал
        страницу на 70px. Плюс `white-space: nowrap` в первой колонке
        юридических таблиц.
      Все три — в эталоне, не в переносе: мобильной вёрстки там просто нет.
- [x] **`/api/v1/` зарезервирован на `corebridge.ru` (2026-07-30).** Раньше его там
      не было — топология в README описывала single-origin, а маршрут отсутствовал.
      Теперь оба домена отвечают одинаково, проверено по четырём эндпоинтам.
      ⚠️ **Главное при правках:** префикс обслуживают **два разных сервиса**.
      `/api/v1/license/*` → license-service:3002, всё остальное → bridge:3001.
      Один общий `location /api/v1/` увёл бы проверку лицензии в bridge, и она
      отвечала бы 404 молча и только на этом домене. Блоки повторяют порядок
      из `api.corebridge.ru.conf`, при правках сверяться с ним.
      ⚠️ **Колбэки внешних сервисов остались только на `api.corebridge.ru`.**
      На `corebridge.ru` `/api/v1/webhooks/` отвечает 404 с указанием верного
      адреса: у колбэка должен быть один адрес, иначе инциденты придётся
      разбирать в двух местах, а wildcard-CORS и лимиты для них заданы там.
      В документации называем один адрес — `api.corebridge.ru`.

Подробности ниже, в разделе 6. Порядок: сначала `pricing` (он уже подключён к
`GET /lk/plans` и нужен для кнопок оплаты), затем лендинг, затем юридические
страницы по `legal_corrections.md`, затем `docs` / `integrations` / `n8n` /
`contacts` / `for-business`.

⚠️ Не забыть: `contacts` — только реальные реквизиты, без банковских;
в `for-business` не переносить блок «Клиенты, с которыми мы работаем»
(выдуманные отзывы); `about.html` не существует — ссылки вести на `/for-business`.

---

**Ниже — уже закрытое.**

### [x] Э7 — Admin (`admin.corebridge.ru`) — закрыт 2026-07-29

Три экрана есть в макете, остальные сверстаны самостоятельно (решение Дмитрия 2026-07-28).

1. **`admin`** — KPI из `GET /admin/stats`, статус сервисов из `GET /admin/health`,
   журнал из `GET /admin/audit` (бейджи по `GET /admin/audit/actions`, не выдумывать).
   ⚠️ Дельты и проценты uptime не показывать — истории нет.
2. **`admin/users`** — ⚠️ в модалку смены тарифа добавить поле **«Причина»**: сервер требует
   обязательный `reason`, в макете его нет. ⚠️ `tenant_status` четырёхзначный:
   `active|blocked|pending_deletion|purged` — интерфейс не должен падать.
3. **`admin/integrations`** — `/admin/n8n/*`.
4. **Очередь обращений по ПДн** — `GET /admin/privacy/requests`, экспорт и удаление тенанта.
5. **Собрать недостающие экраны самим:** тенанты, платежи, очереди/DLQ, версии .epf.
   Макетов нет — верстать по аналогии с `admin-users.html`.

⚠️ **Проверка админки ограничена:** `/admin/*` за IP-whitelist, с сервера прилетает 403.
Данные придётся смотреть через `docker exec corebridge-admin`, а экраны — по вёрстке.

Дальше — **Э8** (публичная статика, SEO, юридические страницы, прайс) и **Э9** (сквозной прогон).

## 6. Бэклог

### Э0 — доделать каркас

- [x] **Э0.1 `middleware.ts`** — сделано 2026-07-28, см. раздел «Сделано».
- [x] **Э0.2 Компоненты из `shell.js`.** Сделано 2026-07-28. ← в работе Перенести `renderSidebar`, `renderTopbar`,
      `renderAdminSidebar`, `renderAdminTopbar`, `renderPublicHeader`, `renderPublicFooter`
      в React с **идентичной разметкой и классами**. `popup.js` → `<Popup>` (портал).
      Источник: `design-source/assets/shell.js`, `design-source/assets/popup.js`.
      ⚠️ В `shell.js` пункт меню заведён с `id: 'integrations-app'`, а страница вызывает
      `renderSidebar('integrations')` — из-за этого пункт не подсвечивается. При переносе исправить.
      ⚠️ Ссылки на `about.html` вести на `/for-business` (страницы `about` не существует).
      ⚠️ `window.CB_USER` → данные из `GET /lk/profile` через React-контекст.
- [x] **Э0.3 Тесты и самопроверка глазами.** Сделано 2026-07-28, см. раздел «Сделано».
- [x] **Э0.4 `sitemap.xml` + `robots.txt`.** Сделано 2026-07-28, см. раздел «Сделано».

**Э0 закрыт полностью.**

### Э1 — Auth ✅ закрыт 2026-07-28, подробности в разделе «Сделано»

- [x] `login` — пароль + **Яндекс ID** (не Google, он удалён) + **кнопка magic-link**,
      которой нет в дизайне, но механика работает. Двухшаговый вход при 2FA:
      `twofactor_required` → экран кода → `POST /lk/auth/login/2fa`. Показывать `attempts_left`.
- [x] `register` — `POST /lk/auth/register`. Тексты «30 дней» → «бессрочный пробный тариф»,
      «5 операций в сутки» → «500 операций в месяц». Убрать «Уже с нами: 1 247 компаний».
- [x] `forgot-password` / `reset-password` — ⚠️ после сброса при включённой 2FA сервер
      **не ставит cookie**, отдаёт `twofactor_required` → вести на тот же экран кода.
- [x] `verify-email` + повторная отправка.
- [x] **`/invite/accept`** — страницы нет в дизайне, но сервер шлёт на неё ссылки в приглашениях.
      Форма «Имя + Пароль» → `POST /lk/users/accept`.
- [x] Убрать отзыв «Игорь П., Ритейл-М» с `login.html` — выдуманный.

### Э2 — Dashboard + epf (всё реальное)

- [ ] `dashboard` — KPI, график `GET /lk/dashboard/activity` (только `7d|30d`, **90д убрать**),
      лента из `GET /lk/logs`. ⚠️ KPI «Успешность синхронизации» источника не имеет —
      считать на клиенте как `ok/(ok+error)` из activity. ⚠️ Дельты («+2 за неделю», «+12 % MoM»)
      **не показывать** — истории нет. ⚠️ `days_left: null` = «Бессрочно», не «0 дней».
- [ ] `epf` — ⚠️ конфигурации: **УТ 11 / УНФ / КА / БП 3.0**, коды `ut11|unf|ka|bp`.
      В дизайне четвёртая карточка «1С:ERP» — такой сборки на сервере нет.
      Полный JWT из `GET /lk/token/full` (owner-only, не-owner → masked + подсказка).
      «Обновить» → `POST /lk/token/refresh`, обработать `402 NO_ACTIVE_SUBSCRIPTION`.
      Убрать из попапа фразу про «PDF-инструкцию на email» — сервер её не шлёт.

### Э3 — Интеграции

- [ ] `my-integrations` — ⚠️ три блока `.preview-block` внизу макета («Представление: таблица»,
      «Состояние: пусто», «Модальное окно») — это **макеты состояний**, не секции экрана.
      Таблица → переключатель вида, пустое → условный рендер, модалка → **удалить** (§8.1).
      «+ Добавить интеграцию» → `/epf`. Кнопка «Обновить токен» по `needs_reauth`,
      действие из `reauth_action` (сейчас всегда `credentials_form` → форма ввода ключей).
      Поля `requests_this_month`, `contractor_name`, `warehouse_name` сервер не отдаёт → «—».
- [ ] `workflows` — `GET /lk/workflows/catalog|executions`, `POST /lk/workflows/activate`.

### Э5 — Settings ✅ закрыт 2026-07-29

- [x] Профиль, **Компания** (секции нет в дизайне, поля на сервере есть),
      **Команда** (секции нет, API готов целиком), Безопасность, Уведомления.
- [ ] ⚠️ **Убрать блок 2FA «через SMS»** — заменить на Telegram. Подписать:
      «Вход через Яндекс ID не требует кода — Яндекс подтверждает личность сам» (согласовано).
      Показывать `recovery_codes_left`. Запретить отвязку Telegram при включённой 2FA (`409`).
- [ ] ⚠️ Убрать поля, которых нет на сервере: аватар с загрузкой, Должность, Язык интерфейса,
      Часовой пояс. Убрать блок **API-ключей `cb_*`** целиком (§8.5).
- [ ] Уведомления: колонки SMS **нет вовсе**. Telegram — два состояния (`available && !linked`
      → «Подключить», `linked` → чекбоксы + «Отвязать»), опрос статуса раз в 2 с.
- [ ] **Экспорт данных / Удаление аккаунта** → `POST /lk/privacy/request`, показывать `ref`
      и срок 30 дней. Исполняет админ.

### Э7 — Admin (`admin.corebridge.ru`)

- [ ] `admin` — KPI из `GET /admin/stats`, статус сервисов из `GET /admin/health`,
      журнал из `GET /admin/audit` (бейджи по `GET /admin/audit/actions`, не выдумывать).
      ⚠️ Дельты и uptime-проценты не показывать — нет истории.
- [ ] `admin/users` — ⚠️ в модалке смены тарифа добавить поле **«Причина»**: сервер требует
      обязательный `reason`, в макете его нет. ⚠️ `tenant_status` теперь четыре значения:
      `active|blocked|pending_deletion|purged` — интерфейс не должен падать.
- [ ] `admin/integrations` — `/admin/n8n/*`, покрыт почти полностью.
- [ ] Очередь обращений по ПДн: `GET /admin/privacy/requests`, экспорт и удаление тенанта.
- [ ] **Собрать недостающие админ-экраны самим** (решение 2026-07-28): тенанты, платежи,
      очереди/DLQ, версии .epf. Макетов нет — верстать по аналогии с `admin-users.html`
      (та же сетка, `.admin-kpi`, `.table`, боковой `.sheet`). API готово целиком.

### Э8 — Статика, SEO, юридические страницы, прайс

- [ ] `index` (лендинг), `pricing`, `docs`, `integrations`, `n8n`, `contacts`, `for-business`
- [ ] `oferta`, `privacy`, `terms` — по `legal_corrections.md`: Германия вместо Нидерландов,
      бессрочный trial с 500 операций/мес, «Профессионал», единый `info@`, адрес убран из оферты.
- [ ] ⚠️ `contacts` — собрать на **реальных** реквизитах, без банковских (решение Дмитрия),
      без блоков «Офис в Москве», карты, «Гендиректора». Telegram-карточку убрать.
      График работы — **показывать**. НДС и ЭДО — **не выводить** (решение 2026-07-28).
- [ ] ⚠️ `for-business` — **не переносить** раздел «Клиенты, с которыми мы работаем»
      (выдуманные отзывы), бейджи SLA/24-7, блок услуг с ценами, партнёрские пункты.
- [ ] `docs` — **решено 2026-07-28:** документируем только API типа 1 (для .epf).
      «Шаг 3» переписать под JWT со страницы `/epf` (убрать `cb_live_*`), «API Reference» —
      под реальные `/api/v1/*` с верным префиксом; `/workflows/{id}/run` и `/usage` убрать.
- [ ] Прайс — только `GET /lk/plans`, кнопка «Оплатить» не работает до Robokassa.
      `mailto:` у Энтерпрайза → форма `POST /lk/contact` с `source: "pricing"`.

### Промт S10 — учёт операций ✅ написан 2026-07-29

- [x] **Написать `Documents/prompts/backend_S10_usage.md`.**
      Сейчас лимит `monthly_operations` есть в каталоге тарифов и в JWT, но **ни одного
      инкремента и ни одной проверки в коде нет** (сервер сам сообщил, `site_S1_S7_RESPONSE.md` §4.4).
      Реально ведётся только `n8n_executions_month` через `platform.usage_counters`.
      Нужно: определить, что считается «операцией», завести счётчик по аналогии с n8n,
      отдавать факт в `GET /lk/dashboard` (и/или отдельным эндпоинтом), включить
      в существующее уведомление `limit_exceeded` (80 % и 100 %).
      Без этого прогресс-бары «Операций X / Y» на дашборде и в биллинге показывать нечего.

### Э4 — Billing ✅ закрыт 2026-07-29, кроме действия оплаты

- [ ] ⚠️ Merchant ID выдадут только после **готового сайта целиком** (кроме оплат и поддержки).
      Поэтому собираем всё, кроме самого действия оплаты: экран нужен для «готового сайта».
- [ ] История платежей, текущий тариф. ⚠️ Ссылки «Чек» и «УПД» **убрать** — сервер документов
      не отдаёт, а УПД у ИП на УСН не выдаётся. Прогресс-бар «Операций» — только лимит без факта.
- [ ] Заглушка на `payment_url: null` нужна **и после** подключения — на случай сбоя платёжки.
- [ ] Явно написать, что автопродления нет: после промо за 10 ₽ доступ просто кончится.

### Локальное тестирование: инструменты и находки (2026-07-30)

- [x] **`tools/icons.mjs`** — ищет отвалившиеся иконки: пустой или нулевой `<svg>`,
      растянутый без размера, фигуру за пределами `viewBox`, `stroke-width` без
      `stroke`, символы вне шрифта, пустые буквенные плашки.
      → **Нашлось глазами, а не автоматикой:** две иконки на «Для бизнеса»
      рисовали мусор. «Производство» — путь из эталона рисует надкусанный круг
      («печенье»), рядом со словом читается как сломанная картинка.
      «ИТ-интеграторы» — круг, перечёркнутый крестом, рядом с «партнёрами»
      читается как знак запрета. Заменены на завод и схему связей.
      Остальные ~70 иконок проверены контактными листами — в порядке.
- [x] **`tools/usecases.mjs`** — 38 проверок по путям человека, а не по факту
      открытия страницы: гость изучает продукт, фильтрует каталог, сравнивает
      тарифы, пишет в компанию, ищет в документации, читает оферту; клиент
      смотрит дашборд, забирает .epf, разбирается со сценариями, проверяет
      лимиты, ходит по настройкам, пишет в поддержку.
      ⚠️ Ничего не меняет на сервере: действия доводятся до последнего шага
      и там останавливаются.
      → **Нашлось:** вкладки настроек были `<a>` без `href` — такой элемент
      не получает фокус и не нажимается с клавиатуры, до половины настроек
      нельзя было добраться без мыши. Заменены на кнопки с ролью вкладки.
      → **Нашлось в самом тесте:** «гостевые» сценарии шли с cookie сессии,
      то есть гостя не проверяли вовсе, хотя выглядело, будто проверяют.
      Теперь публичные сценарии всегда без cookie.

### Э6 — Поддержка: сделано (2026-07-30)

- [x] **`support`.** Тикет-системы в MVP нет, поэтому из макета убраны «Мои тикеты»
      с историей и статусами — пустой список «тикетов» обещал бы систему, которой
      не существует. «База знаний» убрана по той же причине: ссылки вели бы в никуда.
      Осталось: объяснение, как получить помощь, подсказка что приложить к обращению,
      ссылки на документацию и разделы кабинета, и форма → `POST /lk/contact`.
      Обращение получает номер, по нему можно сослаться в переписке.
      ⚠️ Источника `support` в контракте нет — отправляем как `contacts`, в тексте
      обращения помечено, что оно из кабинета.

### Э6 — Support (исходные требования)

- [ ] Минимальная страница: email `info@corebridge.ru` через `POST /lk/contact`
      (`source: "support"`) + ссылка на документацию. Тикеты, чат, база знаний — не собирать.

### Э9 — Сквозной прогон

- [ ] Скриншоты каждой страницы, сверка с `design-source/`, прокликивание кнопок,
      e2e против реального API, проверка cookie/SSL/субдоменов.

---

## 7. Решения Дмитрия (2026-07-28) — вопросы закрыты

- [x] **API делится на три лагеря** — рамка, по которой решаем, что документировать:
      1. **для .epf** — цепочка epf ↔ сайт ↔ n8n ↔ интеграционный сервис (`/api/v1/*`, Bearer JWT);
      2. **для админки** — свои настройки через n8n (`/admin/*`, admin-сессия);
      3. **для работы сайта** — `/lk/*`, cookie-сессия.
      **Следствие для `docs.html`:** публичная документация описывает **только тип 1** — это то,
      с чем работает клиент. Типы 2 и 3 внутренние, наружу не документируем.
      Раздел «Шаг 3. Активировать API-ключом» переписать под **JWT со страницы `/epf`**
      (никаких `cb_live_*`), «API Reference» — под реальные `/api/v1/*` из `API_ENDPOINTS.md`,
      с верным префиксом. Несуществующие `/workflows/{id}/run` и `/usage` убрать.
- [x] **Контакты:** график работы — **показывать**; **НДС — не выводить** (ИП на УСН без НДС,
      выводить нечего); **ЭДО — пока не ставим**.
- [x] **Недостающие админ-экраны** (тенанты, платежи, очереди/DLQ, версии .epf) —
      **реализуем сами**, по аналогии с существующей админ-вёрсткой. API для всех готово.
- [x] **Адрес в `privacy.html` — оставить** как есть. (В оферте убран, там решение другое.)
- [x] **`monthly_operations`: реализовать учёт на сервере**, а на страницах пользователя
      показывать как в чистовом дизайне (прогресс-бары «Операций X / Y»).
      → нужен промт **S10**, см. раздел 6.
- [x] Ст. 18.5 152-ФЗ (БД в Германии) — принято к сведению, вопрос не закрыт, но не блокирует.

### Осталось от Дмитрия

- [ ] Robokassa — после готового сайта (кроме оплат и поддержки).
- [ ] `scripts/` не копируется в Docker-образ admin — передать бэкенду (сейчас обошли своим cron).
- [ ] «Ответим в течение 1 рабочего дня» на форме контактов — оставляем? (не переспрашивал)

---

## 8. Мелочи, которые легко забыть

- **Браузер для тестов ставится отдельно:** `npx playwright install firefox` — в образ не входит,
  без него сорвётся первый же `npm run test:e2e`.
- **Посмотреть на сайт своими глазами:** `npm run inspect` (всё), `npm run inspect -- pricing login`
  (точечно), `CB_SESSION=<cookie> npm run inspect -- lk` (закрытые экраны).
  Скриншоты и отчёт — в `artifacts/`, в git не попадают.
- **Сверка с макетом:** `npm run compare`. Пока `ported: false` в `tests/visual/pages.json`,
  цифра расхождения близка к 100 % — это «экран ещё заглушка», а не поломка.
  Перенёс экран → поставил `ported: true`, и гейт начинает держать вёрстку в допуске.
- **Тесты гоняются против прода** — там ровно та сборка, что в репозитории. Локальный прогон:
  `BASE_URL=http://127.0.0.1:3005`, но тесты админ-субдомена тогда отвалятся: Host в Firefox
  не подменить, а разводку `middleware.ts` делает по нему.
- **MSW стоит, но не разворачивался** и это осознанно: S1–S9 закрыли всё, мокать нечего.
  Появится дырка в API — заводить `lib/mocks/` тогда, а не заранее.
- **Next.js типизирует и тестовые файлы** (`tsconfig` включает `**/*.ts`): ошибка типа
  в `playwright.config.ts` роняет `npm run build`. Уже поймал так `reducedMotion` —
  в Playwright 1.62 он живёт в `contextOptions`, а не рядом с `locale`.
- `npm run dev` и `npm start` слушают **3005** (3000 занят контейнером lk-api).
- Сервис: `sudo systemctl restart corebridge-site`, логи `journalctl -u corebridge-site`.
- nginx-конфиги в `/etc/nginx/sites-available/` — **обычные файлы** (не симлинки),
  автодеплой бэкенда их не трогает. Копии в `deploy/nginx/`.
- `sudo` без пароля доступен.
- Редирект после входа и magic-link сервер ведёт на **`/dashboard`**.
- ~~Приглашения в команду сервер шлёт на `${LK_BASE_URL}/lk/invite/accept`~~ — **закрыто 2026-07-28**:
  был 404, теперь точное совпадение в vhost отдаёт этот путь Next.js, а `middleware.ts`
  переписывает его на `/invite/accept`. Бэкенд трогать не пришлось.
- Ошибка смены пароля у Яндекс-аккаунта — код `OAUTH_ACCOUNT` (не `GOOGLE_ACCOUNT`).
- В каталоге `assets/integrations-data.js` 34 записи, но одна («Иное») не сервис → «33 сервиса».
- `assets/integrations-manager.js` (67 КБ) — инструкции «где взять ключи» для 26 сервисов.
  Модалку каталога убираем, но **инструкции перенести** в форму ввода ключей.
